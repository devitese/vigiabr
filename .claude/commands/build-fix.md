---
description: "Bug fix workflow -- diagnose, fix, verify with tests"
argument-hint: "[#issue-number or error description]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Task
---

# /build fix $ARGUMENTS

Invoke `Skill("fixing-bugs", "$ARGUMENTS")` and follow its instructions.
