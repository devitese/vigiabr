---
description: "Self-review changes, fix issues, create draft PR"
argument-hint: "[--draft|--ready]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, Task, TeamCreate, TaskCreate, TaskUpdate, TaskList, SendMessage
---

# /ship review $ARGUMENTS

Invoke `Skill("reviewing-for-shipping", "$ARGUMENTS")` and follow its instructions.
