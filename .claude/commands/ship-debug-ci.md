---
description: "Debug GitHub Actions CI workflow failures"
argument-hint: "[workflow-name]"
allowed-tools: Read, Bash, Glob, Grep
---

# /ship debug-ci $ARGUMENTS

Invoke `Skill("debugging-ci-failures", "$ARGUMENTS")` and follow its instructions.
