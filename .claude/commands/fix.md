---
description: "Fix bugs -- alias for /build fix"
argument-hint: "[error|file|#issue|--diagnose|--types]"
allowed-tools: Skill
---

# /fix $ARGUMENTS

Thin alias -- routes to `/build fix`.

Invoke `Skill("fixing-bugs", "$ARGUMENTS")` and follow its instructions.
