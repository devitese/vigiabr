---
description: "Full feature implementation -- plan, code, test, iterate"
argument-hint: "[#issue-number or description]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Task, AskUserQuestion, Skill, TeamCreate, TaskCreate, TaskUpdate, TaskList, SendMessage
---

# /build feature $ARGUMENTS

Invoke `Skill("building-features", "$ARGUMENTS")` and follow its instructions.
