---
description: "Read PR feedback, implement fixes, push updates"
argument-hint: "[PR-number]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# /ship respond $ARGUMENTS

Invoke `Skill("responding-to-pr-feedback", "$ARGUMENTS")` and follow its instructions.
