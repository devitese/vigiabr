[project]
name = "vigiabr-pipeline"
version = "0.1.0"
description = "VigiaBR data pipeline — transparency monitoring for Brazilian public officials"
requires-python = ">=3.12"
license = "AGPL-3.0-only"

[tool.uv.workspace]
members = ["schemas", "extraction", "processing"]
